import { RemoveLockLinesPipe } from './remove-lock-lines.pipe';

describe('RemoveLockLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveLockLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
